import { useMemo, useState } from "react";
import { ArrowRight, Bot, CalendarDays, Check, ChevronDown, CircleAlert, FileCheck2, FileText, GripVertical, LoaderCircle, MessageSquareText, MoreHorizontal, Plus, Search, ShieldAlert, ShieldCheck, Trash2, Upload, UserRound, X } from "lucide-react";
const decisions=[{title:"更新林昊当前职位",type:"信息补全",risk:"低风险",confidence:92},{title:"导入 18 篇具身智能论文",type:"学术搜索",risk:"需检查",confidence:86},{title:"覆盖岗位软性要求",type:"岗位解析",risk:"中风险",confidence:81}];
function DToast({text,close}){return text?<div className="dcx-toast"><ShieldCheck/><span>{text}</span><button onClick={close}><X/></button></div>:null}
function DModal({type,close,done}){return <div className="dcx-shade"><section className="dcx-modal" role="dialog"><header><div className={type==="add"?"normal":"danger"}>{type==="add"?<FileCheck2/>:<ShieldAlert/>}</div><span><small>DECISION CONTROL</small><h2>{type==="add"?"新建审核事项":"删除审核记录"}</h2></span></header>{type==="add"?<><label>事项名称<input defaultValue="确认候选人当前公司"/></label><label>业务类型<div><button className="active">信息补全</button><button>岗位解析</button></div></label></>:<p>删除后不能恢复这条审核记录，但已经写入的业务数据不会回滚。</p>}<footer><button onClick={close}>取消</button><button className={type==="add"?"primary":"danger"} onClick={done}>{type==="add"?"创建事项":"确认删除"}</button></footer></section></div>}
function Agent(){const [messages,setMessages]=useState(["解释为什么建议更新林昊的当前职位。","个人主页和最近公开演讲均使用“具身智能算法负责人”，现有字段仍是“算法专家”。两条来源独立且时间更新。"]);const [input,setInput]=useState("");const [busy,setBusy]=useState(false);const [toast,setToast]=useState("");const send=()=>{if(!input.trim())return;setMessages(v=>[...v,input]);setInput("");setBusy(true);setTimeout(()=>{setMessages(v=>[...v,"如果你仍不确定，可以选择暂不写入；该建议会保留在待审核列表。"]);setBusy(false)},650)};return <section className="dcx-page dcx-agent"><header><div><ShieldCheck/><b>Hunter Decisions</b></div><nav><button className="active">待决定</button><button>已完成</button><button>规则</button></nav><span>3 项待处理</span></header><main><aside><small>审核队列</small><h1>待决定事项</h1>{decisions.map((d,i)=><button className={i===0?"active":""} key={d.title}><i>{i===1?<ShieldAlert/>:<FileCheck2/>}</i><span><small>{d.type}</small><b>{d.title}</b><em>{d.risk} · {d.confidence}%</em></span><ArrowRight/></button>)}</aside><section><header><div><small>信息补全</small><h2>更新林昊当前职位</h2></div><em>低风险</em></header><div className="dcx-thread">{messages.map((m,i)=><article className={i%2?"agent":"user"} key={`${m}-${i}`}><i>{i%2?<Bot/>:<UserRound/>}</i><p>{m}</p></article>)}{busy&&<article className="agent"><i><LoaderCircle className="spin"/></i><p>正在重新核验来源与写入影响…</p></article>}</div><footer><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="询问依据、风险或写入影响"/><button onClick={send}><ArrowRight/></button></footer></section><aside className="dcx-evidence"><header><h2>证据与影响</h2><strong>92%</strong></header><article><small>写入前</small><del>算法专家</del><small>写入后</small><b>具身智能算法负责人</b></article>{["个人主页","公开演讲"].map((x,i)=><button key={x}><FileCheck2/><span><b>{x}</b><small>{i?"2026-07-18":"2026-08-01"}</small></span><ArrowRight/></button>)}<footer><button onClick={()=>setToast("建议已拒绝")}>拒绝</button><button className="primary" onClick={()=>setToast("建议已确认写入")}>确认写入</button></footer></aside></main><DToast text={toast} close={()=>setToast("")}/></section>}
function List(){const [q,setQ]=useState("");const [menu,setMenu]=useState(false);const [modal,setModal]=useState("");const [toast,setToast]=useState("");const shown=useMemo(()=>decisions.filter(d=>`${d.title}${d.type}${d.risk}`.includes(q)),[q]);return <section className="dcx-page dcx-list"><header><div><small>DECISION QUEUE</small><h1>审核事项</h1></div><button className="primary" onClick={()=>setModal("add")}><Plus/>新建事项</button></header><div className="dcx-toolbar"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索事项、业务类型或风险"/></label><div><button onClick={()=>setMenu(v=>!v)}>风险级别<ChevronDown/></button>{menu&&<section className="dcx-dropdown" role="menu"><button onClick={()=>setMenu(false)}><Check/>低风险</button><button onClick={()=>setMenu(false)}>需检查</button><button onClick={()=>setMenu(false)}>中风险</button></section>}</div><button><CalendarDays/>近 30 天</button></div><section className="dcx-table"><header><span>审核事项</span><span>业务类型</span><span>风险</span><span>可信度</span><span>证据</span><span>创建时间</span><span>操作</span></header>{shown.map((d,i)=><article key={d.title}><span><i className={i===1?"warn":""}>{i===1?<ShieldAlert/>:<FileCheck2/>}</i><b>{d.title}<small>等待用户决定</small></b></span><span>{d.type}</span><em>{d.risk}</em><strong>{d.confidence}%</strong><span>{i+2} 条</span><time>{i+1} 小时前</time><div><button onClick={()=>setToast("已打开审核详情")}>审核</button><button onClick={()=>setModal("delete")}><Trash2/></button></div></article>)}</section><footer className="dcx-pages"><span>1–3 / 21</span><button disabled>上一页</button><button onClick={()=>setToast("已进入下一页")}>下一页</button></footer>{modal&&<DModal type={modal} close={()=>setModal("")} done={()=>{setToast(modal==="add"?"审核事项已创建":"审核记录已删除");setModal("")}}/>}<DToast text={toast} close={()=>setToast("")}/></section>}
function Pipeline(){const [drag,setDrag]=useState("");const [note,setNote]=useState("");const [toast,setToast]=useState("");const lanes=["待核验","证据完整","等待决定","执行写入","已完成"];return <section className="dcx-page dcx-pipeline"><header><div><small>CONTROLLED RECRUITING FLOW</small><h1>受控招聘流程</h1></div><span><ShieldCheck/>关键阶段需要明确决定</span></header><div className="dcx-board">{lanes.map((l,i)=><section onDragOver={e=>e.preventDefault()} onDrop={()=>{setToast(i===3?"该阶段需要先确认写入":"候选人阶段已更新");setDrag("")}} key={l}><header><b>{l}</b><em>{i+1}</em></header>{["赵星羽","陈松","周雨澄","林昊"].slice(i%4,i%4+1).map(n=><article draggable onDragStart={()=>setDrag(n)} key={n}><header><GripVertical/><i>{n[0]}</i><strong>{[78,83,88,94][i%4]}</strong></header><h2>{n}</h2><p>{i<2?"资料和岗位证据正在核验。":"已通过职级与范围门禁。"}</p><footer><em>{i===0?"需补证":"低风险"}</em><button onClick={()=>setNote(n)}><MessageSquareText/></button></footer></article>)}</section>)}</div>{note&&<div className="dcx-shade"><section className="dcx-modal"><header><span><h2>添加决策备注</h2></span></header><textarea defaultValue={`${note}：确认写入前需要补充当前团队规模。`}/><footer><button onClick={()=>setNote("")}>取消</button><button className="primary" onClick={()=>{setToast("备注已保存");setNote("")}}>保存</button></footer></section></div>}<DToast text={toast} close={()=>setToast("")}/></section>}
function Matching(){const [active,setActive]=useState(0);const [toast,setToast]=useState("");const people=["林昊","周雨澄","陈松"];return <section className="dcx-page dcx-matching"><header><div><small>EVIDENCE REVIEW</small><h1>匹配决策</h1></div><button className="primary" onClick={()=>setToast("匹配决策已提交")}>提交决定</button></header><main><nav>{people.map((p,i)=><button className={active===i?"active":""} onClick={()=>setActive(i)} key={p}><span><b>{p}</b><small>{["算法负责人","VLA 研究员","感知专家"][i]}</small></span><strong>{[94,88,83][i]}</strong></button>)}</nav><article><header><div><small>候选人 {active+1} / 3</small><h2>{people[active]}</h2></div><em>建议推荐</em></header><section className="dcx-scoregrid">{[["技能",96],["职级",92],["方向",95],["证据",89]].map(([a,b])=><div key={a}><span>{a}</span><strong>{b}</strong><i><em style={{width:`${b}%`}}/></i></div>)}</section><section><h3>支持推荐的证据</h3>{["负责过完整 VLA 模型与数据闭环","当前管理范围与岗位相近","公开研究方向与目标问题一致"].map(x=><p key={x}><Check/>{x}</p>)}</section><aside><ShieldAlert/><div><b>写入影响</b><p>确认后候选人将进入“已推荐”阶段，并创建客户跟进任务。</p></div></aside><footer><button onClick={()=>setToast("已标记不推荐")}>不推荐</button><button onClick={()=>setToast("已请求补充证据")}>补充证据</button><button className="primary" onClick={()=>setToast("已确认推荐")}>确认推荐</button></footer></article></main><DToast text={toast} close={()=>setToast("")}/></section>}
function Cards(){const [tab,setTab]=useState("待决定");const [toast,setToast]=useState("");return <section className="dcx-page dcx-cards"><header><div><small>DECISION CASES</small><h1>决策卡片</h1></div><nav>{["待决定","低风险","需检查"].map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav></header><main>{decisions.concat(decisions).map((d,i)=><article key={`${d.title}-${i}`}><header><i className={i%3===1?"warn":""}>{i%3===1?<ShieldAlert/>:<ShieldCheck/>}</i><span><small>{d.type}</small><h2>{d.title}</h2></span><strong>{d.confidence-i}%</strong></header><p>{i%2?"需要确认来源是否足以支持业务写入。":"来源完整，写入影响范围清晰。"}</p><div><span>{d.risk}</span><span>{i%3+2} 条证据</span></div><footer><button onClick={()=>setToast("已打开证据")}>查看证据</button><button onClick={()=>setToast("已进入审核")}>开始审核<ArrowRight/></button></footer></article>)}</main><button className="dcx-more" onClick={()=>setToast("已加载更多决策")}>加载更多</button><DToast text={toast} close={()=>setToast("")}/></section>}
function UploadPage(){const [phase,setPhase]=useState(0);const [toast,setToast]=useState("");const start=()=>{setPhase(1);setTimeout(()=>setPhase(2),750)};return <section className="dcx-page dcx-upload"><header><small>VALIDATED INTAKE</small><h1>上传并校验业务资料</h1><p>格式、内容类型和写入边界必须通过检查后才能进入后续处理。</p></header><main><section><button className="dcx-drop" onClick={start}><Upload/><span><b>选择文件</b><small>PDF、DOCX、XLSX</small></span></button><label>资料用途<div><button className="active">候选人简历</button><button>岗位 JD</button><button>公司资料</button></div></label><button className="primary" onClick={start}>上传并检查</button></section><aside><h2>准入检查</h2>{phase===0?<p>文件上传后显示校验结果。</p>:<>{[["文件格式","通过"],["内容类型","通过"],["必填信息","通过"],["重复数据","发现 1 条"]].map(([a,b],i)=><article className={i===3?"warn":""} key={a}><i>{i===3?<ShieldAlert/>:<Check/>}</i><span><b>{a}</b><small>{b}</small></span></article>)}{phase===1?<LoaderCircle className="spin"/>:<button onClick={()=>setToast("已打开重复数据确认")}>处理重复数据<ArrowRight/></button>}</>}</aside></main><DToast text={toast} close={()=>setToast("")}/></section>}
function Tasks(){const [active,setActive]=useState(0);const [toast,setToast]=useState("");const jobs=["核验林昊职位更新","检查论文批量导入","确认岗位画像覆盖","修正公司资料输出"];return <section className="dcx-page dcx-tasks"><header><div><small>VALIDATION RUNS</small><h1>门禁与写入进度</h1></div><span><ShieldCheck/>所有写入均经过检查</span></header><main><section>{jobs.map((j,i)=><button className={active===i?"active":""} onClick={()=>setActive(i)} key={j}><i className={i===1?"warn":""}>{i===0?<LoaderCircle className="spin"/>:i===1?<ShieldAlert/>:<Check/>}</i><span><b>{j}</b><small>{i===0?"运行中":i===1?"需要修正":"已完成"}</small></span><strong>{[72,48,100,100][i]}%</strong></button>)}</section><article><header><div><small>当前任务</small><h2>{jobs[active]}</h2></div><button onClick={()=>setToast("任务已暂停")}>暂停</button></header>{[["结构校验","已通过"],["业务字段检查","已通过"],["证据关联","运行中"],["写入确认","等待"]].map(([a,b],i)=><section key={a}><i>{i<2?<Check/>:i===2?<LoaderCircle className="spin"/>:i+1}</i><span><b>{a}</b><small>{b}</small>{i===2&&<p>正在确认每个建议都有可访问的来源证据。</p>}</span></section>)}<aside><CircleAlert/><div><b>检查失败时不会写入</b><p>系统会要求 Agent 修正；达到重试上限后停止任务。</p></div></aside><footer><button onClick={()=>setToast("已打开完整检查记录")}>查看检查记录</button></footer></article></main><DToast text={toast} close={()=>setToast("")}/></section>}
const pages={agent:Agent,list:List,pipeline:Pipeline,matching:Matching,cards:Cards,upload:UploadPage,tasks:Tasks};export function DecisionExperience({view}){const Page=pages[view];return Page?<Page/>:<div className="fatal-state">页面不存在。</div>}
