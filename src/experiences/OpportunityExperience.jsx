import { useMemo, useState } from "react";
import { ArrowRight, Bot, Building2, CalendarDays, Check, ChevronDown, CircleAlert, FileSpreadsheet, GripVertical, LoaderCircle, MapPin, MessageSquareText, MoreHorizontal, Plus, Search, Sparkles, Target, Trash2, Upload, UserRound, X, Zap } from "lucide-react";
const opportunities=[{company:"智元新创",type:"招聘扩张",signal:"新增 VLA 与灵巧手岗位",score:94,city:"上海"},{company:"穹境机器人",type:"融资动态",signal:"完成新一轮融资并扩充团队",score:88,city:"北京"},{company:"逐光科技",type:"人才流动",signal:"算法团队出现关键职位空缺",score:83,city:"深圳"}];
function OToast({text,close}){return text?<div className="oppx-toast"><Zap/><span>{text}</span><button onClick={close}><X/></button></div>:null}
function OModal({type,close,done}){return <div className="oppx-shade"><section className="oppx-modal" role="dialog"><header><Target/><div><small>OPPORTUNITY OBJECT</small><h2>{type==="add"?"新建公司机会":"删除机会"}</h2></div></header>{type==="add"?<><label>公司名称<input defaultValue="远川智能"/></label><label>机会来源<input defaultValue="新增招聘岗位"/></label></>:<p>删除后，该机会的自动补全任务会停止，已创建的公司数据会保留。</p>}<footer><button onClick={close}>取消</button><button className={type==="add"?"primary":"danger"} onClick={done}>{type==="add"?"创建机会":"确认删除"}</button></footer></section></div>}
function Agent(){const [msgs,setMsgs]=useState(["帮我判断最近值得 BD 跟进的具身智能公司。","已发现 9 家近期有招聘或融资变化的公司，正在补全岗位和关键联系人。"]);const [input,setInput]=useState("");const [busy,setBusy]=useState(false);const [toast,setToast]=useState("");const send=()=>{if(!input.trim())return;setMsgs(v=>[...v,input]);setInput("");setBusy(true);setTimeout(()=>{setMsgs(v=>[...v,"已按上海地区重新排序，智元新创和远川智能进入高优先队列。"]);setBusy(false)},650)};return <section className="oppx-page oppx-agent"><aside><div><Target/><b>Hunter</b></div><nav>{["机会","公司","岗位","人才"].map((x,i)=><button className={i===0?"active":""} key={x}>{x}</button>)}</nav><section><small>本周新增机会</small><strong>38</strong><span>较上周 +12%</span></section></aside><main><header><div><small>BD 机会 Agent</small><h1>寻找潜在客户</h1></div><button onClick={()=>setToast("已保存为周期任务")}>保存为任务</button></header><div className="oppx-agent-layout"><section><div>{msgs.map((m,i)=><article className={i%2?"agent":"user"} key={`${m}-${i}`}><i>{i%2?<Bot/>:<UserRound/>}</i><p>{m}</p></article>)}{busy&&<article className="agent"><i><LoaderCircle className="spin"/></i><p>正在补全公司、岗位和联系人…</p></article>}</div><footer><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="补充行业、地点或公司阶段"/><button onClick={send}><ArrowRight/></button></footer></section><aside><h2>机会预览</h2>{opportunities.map(o=><button key={o.company}><i><Building2/></i><span><small>{o.type}</small><b>{o.company}</b><p>{o.signal}</p></span><strong>{o.score}</strong></button>)}</aside></div></main><OToast text={toast} close={()=>setToast("")}/></section>}
function List(){const [q,setQ]=useState("");const [menu,setMenu]=useState(false);const [modal,setModal]=useState("");const [toast,setToast]=useState("");const shown=useMemo(()=>opportunities.filter(o=>`${o.company}${o.type}${o.signal}${o.city}`.includes(q)),[q]);return <section className="oppx-page oppx-list"><header><div><small>OPPORTUNITY OBJECTS</small><h1>公司机会</h1></div><button className="primary" onClick={()=>setModal("add")}><Plus/>新建机会</button></header><div className="oppx-toolbar"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索公司、信号、岗位或地点"/></label><div><button onClick={()=>setMenu(v=>!v)}>机会类型<ChevronDown/></button>{menu&&<section className="oppx-dropdown" role="menu"><button onClick={()=>setMenu(false)}><Check/>招聘扩张</button><button onClick={()=>setMenu(false)}>融资动态</button><button onClick={()=>setMenu(false)}>人才流动</button></section>}</div><button><CalendarDays/>近 30 天</button></div><section className="oppx-table"><header><span>公司</span><span>机会信号</span><span>类型</span><span>地点</span><span>机会分</span><span>补全状态</span><span>操作</span></header>{shown.map((o,i)=><article key={o.company}><span><i><Building2/></i><b>{o.company}<small>{i+2} 个相关岗位</small></b></span><p className="has-tooltip" data-tooltip={o.signal}>{o.signal}</p><em>{o.type}</em><span><MapPin/>{o.city}</span><strong>{o.score}</strong><span>{i===1?<LoaderCircle className="spin"/>:<Check/>}{i===1?"补全中":"已补全"}</span><div><button onClick={()=>setToast(`已打开${o.company}`)}>查看</button><button onClick={()=>setModal("delete")}><Trash2/></button></div></article>)}</section><footer className="oppx-pages"><span>1–3 / 38</span><button disabled>上一页</button><button onClick={()=>setToast("已进入下一页")}>下一页</button></footer>{modal&&<OModal type={modal} close={()=>setModal("")} done={()=>{setToast(modal==="add"?"机会已创建":"机会已删除");setModal("")}}/>}<OToast text={toast} close={()=>setToast("")}/></section>}
function Pipeline(){const [drag,setDrag]=useState("");const [note,setNote]=useState("");const [toast,setToast]=useState("");const lanes=["发现信号","信息补全","人工确认","开始联系","转为客户"];return <section className="oppx-page oppx-pipeline"><header><div><small>BD OPPORTUNITY FLOW</small><h1>客户机会泳道</h1></div><button onClick={()=>setToast("已应用自动补全规则")}><Sparkles/>自动补全</button></header><div className="oppx-board">{lanes.map((l,i)=><section onDragOver={e=>e.preventDefault()} onDrop={()=>{setToast(`${drag}已移动到${l}`);setDrag("")}} key={l}><header><b>{l}</b><span>{i+1}</span></header>{opportunities.slice(i%3,i%3+1).map(o=><article draggable onDragStart={()=>setDrag(o.company)} key={o.company}><header><GripVertical/><i><Building2/></i><strong>{o.score}</strong></header><h2>{o.company}</h2><p>{o.signal}</p><div><span>{o.type}</span><span>{o.city}</span></div><footer><small>{i<2?"Agent 处理":"于一凡"}</small><button onClick={()=>setNote(o.company)}><MessageSquareText/></button></footer></article>)}</section>)}</div>{note&&<div className="oppx-shade"><section className="oppx-modal"><header><h2>机会备注</h2></header><textarea defaultValue={`${note}：优先确认当前招聘负责人和开放岗位。`}/><footer><button onClick={()=>setNote("")}>取消</button><button className="primary" onClick={()=>{setToast("备注已保存");setNote("")}}>保存</button></footer></section></div>}<OToast text={toast} close={()=>setToast("")}/></section>}
function Matching(){const [active,setActive]=useState(0);const [toast,setToast]=useState("");return <section className="oppx-page oppx-matching"><header><div><small>COMPANY × TALENT</small><h1>机会匹配</h1></div><button className="primary" onClick={()=>setToast("已创建跟进任务")}>创建跟进</button></header><main><section>{opportunities.map((o,i)=><button className={active===i?"active":""} onClick={()=>setActive(i)} key={o.company}><i><Building2/></i><span><small>{o.type}</small><b>{o.company}</b><p>{o.signal}</p></span><strong>{o.score}</strong></button>)}</section><article><header><div><small>当前机会</small><h2>{opportunities[active].company}</h2></div><em>{opportunities[active].score} 分</em></header><div className="oppx-matchmatrix"><section><span>开放岗位</span><b>6</b><small>VLA、运动控制、灵巧手</small></section><section><span>储备候选人</span><b>24</b><small>其中 8 人高匹配</small></section><section><span>关键联系人</span><b>3</b><small>已找到招聘负责人</small></section></div><h3>建议动作</h3><p>先补充招聘负责人的公开联系方式，再以当前岗位和人才储备作为首次联系依据。</p><footer><button onClick={()=>setToast("已打开公司资料")}>查看公司</button><button onClick={()=>setToast("已进入联系人列表")}>查看联系人</button></footer></article></main><OToast text={toast} close={()=>setToast("")}/></section>}
function Cards(){const [tab,setTab]=useState("高优先");const [toast,setToast]=useState("");return <section className="oppx-page oppx-cards"><header><div><small>OPPORTUNITY CARDS</small><h1>机会卡片</h1></div><nav>{["高优先","待确认","持续跟进"].map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav></header><main>{opportunities.concat(opportunities).map((o,i)=><article key={`${o.company}-${i}`}><header><i><Building2/></i><span>{o.type}</span><button onClick={()=>setToast("机会菜单已打开")}><MoreHorizontal/></button></header><h2>{o.company}</h2><p>{o.signal}</p><div><span><MapPin/>{o.city}</span><strong>{o.score}</strong></div><footer><button onClick={()=>setToast("已稍后处理")}>稍后</button><button onClick={()=>setToast(`已打开${o.company}`)}>查看机会<ArrowRight/></button></footer></article>)}</main><button className="oppx-more" onClick={()=>setToast("已加载更多机会")}>加载更多</button><OToast text={toast} close={()=>setToast("")}/></section>}
function UploadPage(){const [phase,setPhase]=useState(0);const [toast,setToast]=useState("");const start=()=>{setPhase(1);setTimeout(()=>setPhase(2),800)};return <section className="oppx-page oppx-upload"><header><div><small>OBJECT IMPORT</small><h1>批量导入公司线索</h1></div><span>CSV / XLSX</span></header><main><section><button className="oppx-drop" onClick={start}><FileSpreadsheet/><span><b>选择公司线索表</b><small>第一行需要包含字段名称</small></span><Upload/></button><button className="primary" onClick={start}>上传并识别字段</button></section><aside><h2>字段映射</h2>{phase===0?<p>上传后可预览字段映射和数据质量。</p>:<>{[["公司名称","company_name","已识别"],["招聘信号","hiring_signal","已识别"],["联系人","contact","需确认"]].map(([a,b,c],i)=><article key={a}><span><b>{a}</b><small>{b}</small></span><em className={i===2?"warn":""}>{c}</em></article>)}<footer><span>{phase===1?"正在检查 128 行数据":"发现 126 条可导入线索"}</span>{phase===1?<LoaderCircle className="spin"/>:<button onClick={()=>setToast("已打开导入确认")}>继续<ArrowRight/></button>}</footer></>}</aside></main><OToast text={toast} close={()=>setToast("")}/></section>}
function Tasks(){const [active,setActive]=useState(0);const [toast,setToast]=useState("");const jobs=["补全 38 家公司资料","寻找 24 位招聘联系人","核验 16 条招聘信号","生成本周 BD 建议"];return <section className="oppx-page oppx-tasks"><header><div><small>ENRICHMENT RUNS</small><h1>机会补全进度</h1></div><button onClick={()=>setToast("已创建补全任务")} className="primary"><Plus/>新建任务</button></header><main><nav>{jobs.map((j,i)=><button className={active===i?"active":""} onClick={()=>setActive(i)} key={j}><i className={`s${i}`}>{i===0?<LoaderCircle className="spin"/>:i===2?<CircleAlert/>:<Check/>}</i><span><b>{j}</b><small>{i===0?"运行中":i===2?"部分失败":"已完成"}</small></span><strong>{[68,100,76,100][i]}%</strong></button>)}</nav><article><header><div><small>当前任务</small><h2>{jobs[active]}</h2></div><button onClick={()=>setToast("任务已暂停")}>暂停</button></header><div className="oppx-runbar"><i><span style={{width:`${[68,100,76,100][active]}%`}}/></i><b>{[68,100,76,100][active]}%</b></div>{[["公司基础资料","36 / 38"],["招聘岗位","112 个"],["关键联系人","21 人"],["失败来源","2 个"]].map(([a,b],i)=><section key={a}><i>{i===3?<CircleAlert/>:<Check/>}</i><span><b>{a}</b><small>{b}</small></span>{i===3&&<button onClick={()=>setToast("失败来源已重试")}>重试</button>}</section>)}</article></main><OToast text={toast} close={()=>setToast("")}/></section>}
const pages={agent:Agent,list:List,pipeline:Pipeline,matching:Matching,cards:Cards,upload:UploadPage,tasks:Tasks};export function OpportunityExperience({view}){const Page=pages[view];return Page?<Page/>:<div className="fatal-state">页面不存在。</div>}
