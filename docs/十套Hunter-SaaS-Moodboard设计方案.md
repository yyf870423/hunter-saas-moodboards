# Hunter SaaS 十套 Moodboard 设计方案

## 1. 目标与边界

本轮以“科技产品 + 专业猎头服务”为共同底色，保留十套肉眼可辨的产品方向。差异不依赖换色，而来自工作模型、信息架构、组件几何、材质、反馈方式和动效语义。

共同边界：

- 不采用纯开发工具、监控大屏或赛博朋克风格。
- 控件必须有足够厚度、边界和点击面积，避免过细、过轻、漂浮。
- 不展示思维导图、关系网络或其他连线图。
- 每套覆盖 Dashboard 与完整组件页面，组件页面包含表单、选择、数据列表、业务资产、状态、Modal、Drawer、Toast 和四种专属动效。
- Dashboard 和组件页面在十套方案中分别使用独立 React DOM。
- 每套组件页面独立维护下拉框、复选、多选、日期、上传、Tabs、Modal、Drawer、Toast 和动效状态。
- 方案之间仅共享静态演示数据与 Lucide 图标库，不共享组件 DOM、交互状态或动效实现。

## 2. 十套方向

| 编号 | 方向 | 工作模型 | 组件设计语言 | 四种专属动效 | 参考范例 |
|---|---|---|---|---|---|
| 01 | 静默运营台 | 高频批量处理台账 | 3px 小圆角、紧凑行高、明确表头、稳定操作位 | 行聚焦、批次确认、数字计数、状态推入 | Linear、Ashby、Stripe Dashboard |
| 02 | 高管简报 | 先结论后证据的顾问简报 | 编辑式章节、衬线标题、边注、双线与页码 | 章节揭示、重点划线、批注滑入、结论盖章 | McKinsey Insights、Monocle、Granola |
| 03 | 客户会客厅 | 围绕客户承诺与服务节奏 | 深色框架、服务卡、柔和但厚实的控件 | 帷幕进入、服务聚焦、关系提示、通知浮现 | Mercury、Affinity CRM、Pitch Rooms |
| 04 | 人才纪事 | 沿职业变化和沟通记录工作 | 人物档案、时间轴、圆形标记、叙事区块 | 时间线生长、记录钉入、人物翻面、提醒浮现 | Read.cv、Notion Calendar、Granola |
| 05 | 沟通工作台 | 会话、日程与下一步操作连续衔接 | 收件箱、会话气泡、圆角输入、联系人抽屉 | 消息进入、输入状态、会话扫入、日历确认 | Intercom Inbox、Front、Superhuman |
| 06 | 瑞士网格 | 以编号、比例和硬网格建立秩序 | 2–4px 黑线、零圆角、强字重、原色状态块 | 网格擦入、区块弹扣、反色切换、审核击打 | International Typographic Style、Vercel、Pitch |
| 07 | 无纸化工作室 | 材料阅读、批注和归档 | 纸张层叠、页签、下划线输入、批注便笺 | 材料滑入、翻页、便笺落下、归档盖章 | Dovetail、Notion、Pitch Editor |
| 08 | 智能副驾 | 在业务上下文中处理可解释建议 | 建议卡与业务字段并排、证据说明、采纳/拒绝 | 建议浮现、采纳合并、拒绝收起、依据展开 | Intercom Copilot、Granola、Sana Agents |
| 09 | 自动化流水线 | 清晰表达自动执行、人工确认与恢复 | 厚阶段块、强状态带、机械式控件、Checkpoint | 步骤推进、失败回退、门禁解锁、批次瀑布 | Linear Workflows、Asana Workflows、Temporal UI |
| 10 | 证据审核台 | 对比当前值、建议值与来源后裁决 | 编号来源、左右差异、规则线、审核印章 | 差异揭示、来源聚焦、裁决盖章、证据扫描 | Dovetail、Hebbia Matrix、Stripe Identity |

## 3. 完整组件范围

每套组件页面必须用自己的 DOM 和交互逻辑覆盖：

1. 命令：主按钮、次按钮、危险按钮、禁用按钮、分裂按钮及菜单。
2. 输入：搜索、普通输入、校验错误、多行文本、标签、文件上传。
3. 选择：单选下拉、多选下拉、日期/时间、Checkbox、Radio、Toggle、Slider。
4. 数据：Tabs、列表或表格、状态、分数、分页或记录切换。
5. 业务：候选人、岗位、公司、Agent 任务、证据或沟通记录。
6. 浮层：Modal、Drawer、Toast，包含打开和关闭交互。
7. 动效：四个与该方案工作模型一致的独立触发器和动画结果。

## 4. 响应式要求

- 16:9 宽屏：保持完整工作布局，核心内容不做营销式放大。
- iPad：次要区域可下移，复杂表格允许容器内横向滚动，页面本身不能溢出。
- iPhone：多栏改为单栏或可控的内部滚动，不丢失关键命令、状态和浮层入口。
- 所有视口下文本不得遮挡，按钮点击区域保持稳定，弹层不得超出屏幕。

## 5. 验收标准

1. 隐去颜色和名称后，仍可根据布局与组件几何区分十套方案。
2. 十套 Dashboard 根 DOM 与十套组件根 DOM 均不重复。
3. 任一方案的交互状态变化不影响其他方案。
4. 下拉框可再次点击关闭、点击外部关闭、选择后关闭；多选支持继续选择并由“完成”关闭。
5. Checkbox 勾选图标在水平和垂直方向居中。
6. 不存在思维导图、关系网络和连线图组件。
7. 桌面、iPad、iPhone 共 60 个页面组合无脚本错误或页面横向溢出。
8. 首页使用每套真实 Dashboard 截图，不使用与实际内容不一致的占位图。
